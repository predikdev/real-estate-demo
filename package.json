{
  "name": "real-estate",
  "version": "1.0.0",
  "workspaces": [
    "web",
    "studio"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:studio": "npm run dev --workspace=studio",
    "build:web": "npm run build --workspaces=web",
    "build:studio": "npm run build --workspaces=studio"
  }
}
